<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema principal de la app -->
    <style name="Theme.Eventify" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimario</item>
        <item name="colorPrimaryVariant">@color/colorSecundario</item>
        <item name="colorOnPrimary">@color/colorBlanco</item>

        <item name="colorSecondary">@color/colorSecundario</item>
        <item name="colorOnSecondary">@color/colorBlanco</item>

        <item name="android:colorBackground">@color/colorFondo</item>
        <item name="android:textColor">@color/colorTexto</item>

        <!-- Asignamos los estilos personalizados -->
        <item name="android:buttonStyle">@style/EventifyButton</item>
        <item name="android:editTextStyle">@style/EventifyEditText</item>
    </style>
</resources>
